[project]
name = "crypto-dashboard"
version = "0.1.0"
dependencies = ["streamlit", "pandas", "requests"]

[project.dependencies]
dependencies = [
    "streamlit>=1.55",
    "pandas>=2.3.3",
    "requests>=2.32.5",
    "plotly>=6.6.0"
]